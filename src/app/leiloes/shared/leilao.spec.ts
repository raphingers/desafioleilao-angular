/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {Leilao} from './leilao';

describe('Leilao', () => {
  it('deve criar uma instancia', () => {
    expect(new Leilao()).toBeTruthy();
  });
});
